CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

SET(INCLUDE_DIR ${LOGIC_SOURCE_DIR}/link)

INCLUDE_DIRECTORIES(${COMMON_INCLUDE_DIR} ${INCLUDE_DIR})

AUX_SOURCE_DIRECTORY(${CMAKE_CURRENT_SOURCE_DIR} SRC)

ADD_LIBRARY(logic_config ${SRC})
